# build output
bin/
obj/
*.user

# generated test media and the playlists that point at it (see tools/make-samples.ps1)
samples/*.mp4
samples/*.mvp

# per-playlist thumbnail cache
.multiplayer/

# agent scratch: worktrees and session state, none of which belongs in a public repo
.claude/
